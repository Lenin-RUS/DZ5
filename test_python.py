import math
print(math.pi, math.sqrt(4), math.pow(2,2), math.hypot(3,4))
a=(6,4,7,9,3)
print(sorted(a))
print(list(map(lambda x: x**x, a)))
print(list(filter(lambda x: x==4, a)))

def test_pi:
    assert

